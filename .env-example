# The Homies App Environment Variables

# Application Settings
NODE_ENV=production
PORT=3000
# Set to 'true' ONLY in development to bypass auth (NEVER in production)
ALLOW_DEV_AUTH=false

# Email Settings (for your dummy ProtonMail account)
EMAIL_USER=your_dummy_protonmail@proton.me
EMAIL_PASSWORD=your_password

# Supabase Settings (REQUIRED for all environments)
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_key

# MEGA Storage Settings (for secure file storage and backup)
MEGA_EMAIL=your_mega_email
MEGA_PASSWORD=your_mega_password

# Authentication settings
# Set to 'true' to require email verification for all new accounts
REQUIRE_EMAIL_VERIFICATION=true

# Optional custom SMTP settings (rarely needed)
# SMTP_HOST=custom_smtp_host
# SMTP_PORT=custom_smtp_port
# SMTP_SECURE=true_or_false
